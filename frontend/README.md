# Gateway client

This is a universal React Native / Expo app for Android, iOS and web.

See the [project setup guide](../README.md) for local preview accounts, API configuration, mobile builds, migration, roles and security controls.

`npm run web` starts the browser app. `npm start` starts Expo for mobile. `npm run build` exports web files; `npm run build:mobile` validates Android and iOS bundles.
